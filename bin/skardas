#!/usr/bin/env python3
import sys
from skardas import skardas
import logging

logging.basicConfig(stream=sys.stderr, level=logging.DEBUG)

assert(len(sys.argv) == 3)
skardas.run(float(sys.argv[1]), float(sys.argv[2]))
